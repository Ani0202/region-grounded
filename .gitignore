__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.DS_Store
.venv/
env/

# Local runs
outputs/
checkpoints/
data/
wandb/
runs/
*.log

# Generated artifacts
*.parquet
*.jsonl
*.tar
*.tar.gz

# Downloaded model weights (re-download via HuggingFace)
notebooks/florence2_local/

# Intermediate visualizations (regenerated by notebooks)
notebooks/florence2_viz.png

# Draft / scratch docs (not for GitHub)
notes.md
report.md
todo.md
intermediate_report.*

# Claude project metadata
.claude/

# Large binary outputs committed intentionally: output.png (used in LaTeX figure)

# LaTeX build artifacts (regenerated by pdflatex)
report/*.aux
report/*.log
report/*.out
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz
report/*.toc
report/*.bbl
report/*.blg



# NeurIPS style file (download from neurips.cc — not ours to commit)
report/neurips_2024.sty

# Viz PNGs in report/ (copies from notebooks/outputs, regenerated by notebook 10)
report/pg_viz_*.png

# Model weights (large binaries — re-download or re-train)
weights/

# Scratch / exploratory files
random/

# (Claude internal files are excluded by .claude/ above)
